export { default as middleware } from './middleware'
export { default as log } from './logger'

// TODO -- make this file export default a function that takes app and sets up the entire server basially
