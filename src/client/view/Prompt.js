import { h } from 'hyperapp' // eslint-disable-line

export default props =>
  <section id='prompt'>
    <h3>
      Select a post on the left.
      <br />
      Or you can <span>start a new one today</span>.
    </h3>
  </section>
